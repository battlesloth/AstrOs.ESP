# Firmware-version compatibility gating — Phase 1 (firmware)

## Context

AstrOs.Server has no way today to know what firmware version is running on each ESP32 controller. This is the firmware-side phase of a cross-repo feature: each ESP will start reporting its firmware version on every `POLL_ACK` so the server can hard-code a `MINIMUM_FIRMWARE_VERSION` and gate UI based on it.

After this phase merges, controllers will report their firmware version. The server (still running pre-Phase 2 code) ignores the extra field harmlessly; nothing visibly changes for the end user yet. Phase 2 (server + Vue) lands separately in the AstrOs.Server repo on branch `feature/firmware-version-gating` and is what activates the new visual states.

## Decisions (confirmed with user)

- **Wire format**: extend `POLL_ACK` payload only; leave `REGISTRATION_SYNC_ACK` alone. Each ESP responds to its own poll, so each controller's own POLL_ACK is the natural carrier — no ESPNOW mesh changes. Brief unknown-version window after registration is acceptable; the first poll cycle resolves it.
- **Initial version**: bump `VERSION` to `1.2.0`. Phase 2 will hard-code `MINIMUM_FIRMWARE_VERSION = '1.2.0'`, so this firmware will be the first compatible build.
- **Source of truth**: use `AstrOsConstants::Version` (auto-generated by `scripts/version_gen.py` from the `VERSION` file). No new constants needed.

## Wire-format change

`POLL_ACK` payload today: `mac<US>name<US>fingerprint`
`POLL_ACK` payload after: `mac<US>name<US>fingerprint<US>version`

`<US>` = `0x1F`. Append-only.

## Files to modify

- `VERSION` — bump from `1.1.0` → `1.2.0`. The PlatformIO pre-build hook (`scripts/version_gen.py`) regenerates `lib_native/AstrOsUtility/src/version_generated.hpp` automatically.
- `lib_native/AstrOsMessaging/src/AstrOsSerialMessageService.cpp` — extend `getPollAck()` (lines ~124–131):
  ```cpp
  ss << macAddress << UNIT_SEPARATOR << controller << UNIT_SEPARATOR
     << fingerprint << UNIT_SEPARATOR << AstrOsConstants::Version;
  ```
  Add `#include "AstrOsConstants.hpp"` at the top of the file (verify it isn't already pulled in transitively).
- `test/test_native/astros_serial_messages_tests.cpp` — update the POLL_ACK test (search for `getPollAck`) so the expected string includes the new `<US>version` suffix.

## Tasks

- [x] Bump `VERSION` to `1.2.0`; verify `version_generated.hpp` regenerates on next build.
- [x] Append `AstrOsConstants::Version` to `getPollAck()` payload.
- [x] Update native unit test for `getPollAck` to assert the new 4-field format.
- [x] Run `pio test -e test` to confirm tests pass (190 / 190 passed).
- [ ] Flash firmware to a bench ESP and confirm via serial monitor that POLL_ACK frames contain the version suffix.
- [ ] Open PR to AstrOs.ESP `develop`.

## Verification

- Native test `astros_serial_messages_tests.cpp` passes with the updated expectation.
- Manually inspect a serial trace: a POLL_ACK frame should look like `<int>RS<validation>RS<msgid>GS<mac>US<name>US<fingerprint>US1.2.0\n`.
- Flash to bench rig; confirm both master and peer controllers each include their own version in subsequent POLL_ACKs.

## PR feedback addendum — forwarding bug fix

The first cut of this change appended `AstrOsConstants::Version` directly inside `getPollAck`. That was wrong: `getPollAck` runs on the master and is invoked both for the master's own POLL_ACK *and* when the master forwards a padawan's POLL_ACK over serial (see `AstrOsEspNowService::handlePollAck` → `sendToInterfaceQueue(SEND_POLL_ACK, ...)` → `main.cpp:830` → `sendPollAckNak` → `getPollAck`). Hard-coding the local version meant peers always reported the master's version to the server.

Fix: thread the peer's actual version end-to-end:

- ESP-NOW POLL_ACK payload (peer → master) gains a 4th field carrying the peer's `AstrOsConstants::Version`. Peer side: `AstrOsEspNowService.cpp` `sendPollAck` now serializes `name<US>fingerprint<US>version` (master prepends mac → 4 wire fields total).
- Master parses the optional 4th field in `handlePollAck` and packs `fingerprint<US>version` into the interface queue's `message` field (legacy peers without a version send just `fingerprint`).
- `main.cpp` SEND_POLL_ACK dispatcher splits the message field and threads version through to `sendPollAckNak`.
- `getPollAck`, `sendPollAckNak` signatures gained a `firmwareVersion` parameter. Master self-poll path (`main.cpp:432`) now passes `AstrOsConstants::Version` explicitly.
- Native test asserts the version reported is whatever the caller passed (literal `9.9.9`), not `AstrOsConstants::Version`, so the contract that "the version belongs to the same controller as the identifiers" is enforced by the test.

Backward compatibility: a Phase-1+ master receiving a legacy peer's 3-field POLL_ACK forwards an empty version → server treats as incompatible → red exclamation. A legacy master receiving a Phase-1+ peer's 4-field POLL_ACK ignores the trailing field → server gets a 3-field POLL_ACK → same outcome. Mixed-version mesh is safe in either direction during rollout.

## Critical files reference

| Concern                 | File                                                                              |
| ----------------------- | --------------------------------------------------------------------------------- |
| Version source of truth | `VERSION` + `lib_native/AstrOsUtility/src/version_generated.hpp` (auto-generated) |
| POLL_ACK builder        | `lib_native/AstrOsMessaging/src/AstrOsSerialMessageService.cpp:124–131`           |
| Native test             | `test/test_native/astros_serial_messages_tests.cpp`                               |

## Out of scope

- Adding firmware version to `REGISTRATION_SYNC_ACK` (would require ESPNOW-level peer→master version exchange; deferred — POLL_ACK fills the gap within one poll cycle).
- Server-side changes — those land in Phase 2 in the AstrOs.Server repo (`feature/firmware-version-gating`).
